/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface ApiErrorResponse {
  error: string;
}

export type UserRegistrationGender =
  (typeof UserRegistrationGender)[keyof typeof UserRegistrationGender];

export const UserRegistrationGender = {
  male: "male",
  female: "female",
} as const;

export type UserRegistrationGoal =
  (typeof UserRegistrationGoal)[keyof typeof UserRegistrationGoal];

export const UserRegistrationGoal = {
  loss: "loss",
  maintain: "maintain",
  gain: "gain",
} as const;

export type UserRegistrationActivity =
  (typeof UserRegistrationActivity)[keyof typeof UserRegistrationActivity];

export const UserRegistrationActivity = {
  none: "none",
  low: "low",
  medium: "medium",
  high: "high",
} as const;

export interface UserRegistration {
  email: string;
  name: string;
  /** @minLength 6 */
  password: string;
  gender: UserRegistrationGender;
  age: number;
  height: number;
  weight: number;
  goal: UserRegistrationGoal;
  activity: UserRegistrationActivity;
}

export interface LoginCredentials {
  email: string;
  password: string;
}

export interface UserPublic {
  id: number;
  email: string;
  name: string;
  gender: string;
  age: number;
  height: number;
  weight: number;
  goal: string;
  activity: string;
  targetCalories: number;
  targetProtein: number;
  targetFat: number;
  targetCarbs: number;
  targetFiber: number;
  /** Number of photo analysis credits remaining. */
  photoCredits: number;
}

export interface AuthResult {
  token: string;
  user: UserPublic;
}

export type ProfileUpdateGender =
  (typeof ProfileUpdateGender)[keyof typeof ProfileUpdateGender];

export const ProfileUpdateGender = {
  male: "male",
  female: "female",
} as const;

export type ProfileUpdateGoal =
  (typeof ProfileUpdateGoal)[keyof typeof ProfileUpdateGoal];

export const ProfileUpdateGoal = {
  loss: "loss",
  maintain: "maintain",
  gain: "gain",
} as const;

export type ProfileUpdateActivity =
  (typeof ProfileUpdateActivity)[keyof typeof ProfileUpdateActivity];

export const ProfileUpdateActivity = {
  none: "none",
  low: "low",
  medium: "medium",
  high: "high",
} as const;

export interface ProfileUpdate {
  name?: string;
  gender?: ProfileUpdateGender;
  age?: number;
  height?: number;
  weight?: number;
  goal?: ProfileUpdateGoal;
  activity?: ProfileUpdateActivity;
}

export interface FoodEntryInput {
  /** Date in YYYY-MM-DD format */
  date: string;
  dishName: string;
  ingredients: string[];
  calories: number;
  protein: number;
  fat: number;
  carbs: number;
  fiber: number;
  portionWeight: number;
  /** @nullable */
  imageUrl?: string | null;
}

export interface FoodEntryRecord {
  id: number;
  userId: number;
  date: string;
  dishName: string;
  ingredients: string[];
  calories: number;
  protein: number;
  fat: number;
  carbs: number;
  fiber: number;
  portionWeight: number;
  /** @nullable */
  imageUrl?: string | null;
  createdAt: string;
}

export interface FoodImageInput {
  /**
   * Base64-encoded image data without the data URL prefix.
   * @minLength 1
   */
  imageBase64: string;
  /**
   * Image MIME type, e.g. image/jpeg, image/png.
   * @minLength 1
   */
  mimeType: string;
}

export interface FoodAnalysis {
  /** Recognized dish name in Russian. */
  dishName: string;
  /** List of visible main ingredients in Russian. */
  ingredients: string[];
  /** Estimated calories per portion (kcal). */
  calories: number;
  /** Estimated protein per portion (grams). */
  protein: number;
  /** Estimated fat per portion (grams). */
  fat: number;
  /** Estimated carbohydrates per portion (grams). */
  carbs: number;
  /** Estimated dietary fiber per portion (grams). */
  fiber: number;
  /** Estimated portion weight in grams. */
  portionWeight: number;
}

export interface AnalyzeRecipeRequest {
  /**
   * Base64-encoded image data, without the data URL prefix.
   * @minLength 1
   */
  imageBase64: string;
  /**
   * Image MIME type, e.g. image/jpeg, image/png, image/webp.
   * @minLength 1
   */
  mimeType: string;
}

export interface RecipeIngredient {
  name: string;
  /** Human-readable quantity such as "2 tbsp" or "200 g". */
  quantity: string;
}

export interface NutritionFacts {
  /** Calories per serving, formatted with units (e.g. "520 kcal"). */
  calories: string;
  /** Protein per serving, formatted with units (e.g. "32 g"). */
  protein: string;
  /** Fats per serving, formatted with units (e.g. "18 g"). */
  fats: string;
  /** Carbohydrates per serving, formatted with units (e.g. "60 g"). */
  carbs: string;
}

export interface AnalyzedRecipe {
  dishName: string;
  /** Estimated cuisine type, e.g. "Italian", "Japanese". */
  cuisine: string;
  /** Short, evocative description of the dish. */
  description: string;
  /** Total cooking time formatted with units (e.g. "45 minutes"). */
  cookingTime: string;
  /** Difficulty level (e.g. "Easy", "Medium", "Hard"). */
  difficulty: string;
  /** Serving size description (e.g. "Serves 4"). */
  servingSize: string;
  ingredients: RecipeIngredient[];
  /** Ordered cooking steps. */
  steps: string[];
  nutrition: NutritionFacts;
}

export type GetFoodEntriesParams = {
  /**
   * Filter by date (YYYY-MM-DD). If omitted, returns last 30 days.
   */
  date?: string;
};
